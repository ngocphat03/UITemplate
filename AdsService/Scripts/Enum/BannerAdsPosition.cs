#if ADS_SERVICE
namespace UITemplate.AdsService.Scripts.Enum
{
    public enum BannerAdsPosition
    {
        Top,
        Bottom,
        TopLeft,
        TopRight,
        BottomLeft,
        BottomRight,
    }
}
#endif